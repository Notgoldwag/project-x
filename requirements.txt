# === Core Web Framework ===
Flask==3.0.3
python-dotenv==1.0.1
requests==2.32.3

# === Minimal LangChain (remove the heavy parts) ===
langchain-core>=0.1.0        # Core functionality only (~10-20 MB)
langchain-openai>=0.0.5      # OpenAI integration (~5-10 MB)
openai>=1.3.0               # Direct OpenAI API (~5-10 MB)
pydantic>=2.5.0             # Required by LangChain (~10-15 MB)

# === Deployment ===
gunicorn==22.0.0
# uvicorn==0.30.6            # Remove - not needed for Flask
