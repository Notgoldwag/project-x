# === Core Web Framework ===
Flask==3.0.3
python-dotenv==1.0.1
requests==2.32.3

# === Hugging Face (for API calls) ===
huggingface_hub>=0.20.0

# === Machine Learning / NLP (only for local testing) ===

# === Optional (for model training & metrics visualization) ===
tqdm==4.66.5
matplotlib==3.9.2
seaborn==0.13.2

# === Deployment (optional, for Vercel/Render) ===
gunicorn==22.0.0
# uvicorn==0.30.6            # Remove - not needed for Flask

